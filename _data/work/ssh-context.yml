---
name: ssh-context
language: bash
url: https://github.com/rakshazi/ssh-context
description: >
  Bash wrapper around ssh which provides you ability to use contexts (as in kubectl) for SSH.<br />
  Project = context. Context = separate ssh config, keys, etc. Use full power of isolated ssh config
